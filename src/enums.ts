export enum ComponentType {
  default = 0, // 默认显示组件
  business, // 业务组件
  customize, // 用户自定义组件
}

export enum EventType {
  click = 'click',
  change = 'change',
}

export enum ActionType {
  dialog = 'dialog',
  openUrl = 'openUrl',
}